# Hyper OS — target preparation job.
# Strips live-only units, initramfs hooks and pacman hooks from the
# copied filesystem so the installed system boots cleanly as a normal
# Arch install (hyper-live-setup.service is a no-op anyway).
# Hyper OS — SDDM, default session is Hyper (Hyprland).
---
displaymanagers:
  - sddm

defaultDesktopEnvironment:
  executable: "/usr/local/bin/hyper-session"
  desktopFile: "hyper"

basicSetup: false
sysconfigSetup: false

sddm:
  configuration_file: "/etc/sddm.conf"
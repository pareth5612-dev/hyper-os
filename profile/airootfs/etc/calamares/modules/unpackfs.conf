# Hyper OS — offline install: the live system's root filesystem is
# shipped as the squashed airootfs image and copied onto the target.
---
unpack:
  -   source: "/run/archiso/bootmnt/hyper/x86_64/airootfs.sfs"
      sourcefs: "squashfs"
      destination: ""